# Legacy/disclosed statistical script

Before the archival release, copy the previously disclosed file
`circular_shift_validation_REVISED.py` into this directory unchanged.

The new `code/circular_shift_validation_from_csv.py` independently implements
the same null model for generated CSV root catalogues. Keeping the original
script allows exact comparison with the workbook-based analysis reported in the
first manuscript.
