# Data Availability wording for both manuscripts

## Use only after a validated Zenodo v1.0.0 release

The complete computational workflow, including NASA/JPL Horizons query scripts, fixed query parameters, numerical tolerances, root-finding procedures, the full unselected root catalogue, statistical validation code, provenance tables, and frozen reproducible outputs, is archived at Zenodo: **[INSERT DOI]**. The repository provides computational reproducibility for the astronomical component of both companion studies. Historical and mythological assignments remain interpretive and are documented separately in the provenance table.

## Use if publishing a v0.9.0 reconstruction before full validation

The reconstructed computational workflow, fixed query parameters, numerical procedures, disclosed root catalogue, statistical validation code, and provenance tables are archived at Zenodo: **[INSERT DOI]**. The release makes the workflow auditable but is explicitly labelled as awaiting complete independent end-to-end validation of the JPL Horizons root-generation stage. Historical and mythological assignments remain interpretive and are documented separately in the provenance table.
